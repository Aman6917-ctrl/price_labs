"""
RAG package.

Runtime pipeline modules:
  loaders/     — pluggable document sources
  chunking.py  — text splitting + chunk metadata
  embeddings.py
  vectorstore.py
  ingestion.py — orchestrator used by CLI and HTTP
"""
